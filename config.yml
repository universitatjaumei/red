red:
  webserver:
      domain: haddock.si.uji.es
      port: 5000
      users:
        - vrubert
        - borillo
  database:
    db_file: /data/db/red.db

  nginx:
    red_nginx_conf_file: /data/nginx.conf.d/red.conf
    hostname: nginx
    socket: /var/run/docker.sock

  dns:
    api_url: http://dns.uji.es/api
